import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  flex-direction: column;

  button {
    border: none;
    background: none;
    align-self: flex-end;
  }
`;
